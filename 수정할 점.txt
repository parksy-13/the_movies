- css 따로 빼두기
- 재할당 없으면 const로 제작
- 이미지 문제: 현재 파일의 위치를 기준으로 경로 적기
- 스네이크 명칭 -> 카멜로 변경
- git에 올릴 파일을 구분할 수 있는 방법과 api키를 따로 관리하는 방법 적용
- git에서 develop 브랜치를 따서 master/main으로 pr하고 merge하는 경험
- 검색시 enter키도 되도록
- hover에 마우스 커서 표시하기